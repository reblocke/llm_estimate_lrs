# Large Language Models and Diagnostic Likelihood Ratios

This repository contains the analysis code and curated workbooks for the manuscript:

**Large Language Models Generate Diagnostic Likelihood Ratios with Low Mean Bias but Wide Dispersion**

The study compares likelihood-ratio (LR) outputs generated by large language models (LLMs) with literature-reported diagnostic LRs curated from TheNNT.com. The repository is intended to support reproducibility of the manuscript analyses and additional requested sensitivity analyses. Generated manuscript figures, tables, draft documents, scratch notebooks, and historical working files are not tracked in the source release.

## Repository Contents

```text
.
├── data_analysis.ipynb
├── lr_scraper_estimator.ipynb
├── additional_requested_analyses.ipynb
├── threshold_perturbation_sensitivity_analysis.ipynb
├── NNT_LRs_08-26-2025.xlsx
├── nnt_lrs_with_estimated.xlsx
├── pyproject.toml
├── uv.lock
├── .env.example
├── LICENSE
└── README.md
```

### Notebooks

- `data_analysis.ipynb` reproduces the main manuscript analyses, figures, Bland-Altman agreement summaries, qualitative agreement tables, and inverse reliability-calibration outputs from the prepared workbook.
- `additional_requested_analyses.ipynb` reproduces additional analyses that do not require new model calls, including reliability zones, shortcut baselines, true LR+ versus LR- stratification, posterior-impact summaries, and the descriptive GPT-5 laboratory discrepancy table.
- `threshold_perturbation_sensitivity_analysis.ipynb` runs the exploratory GPT-5 threshold-perturbation analysis. Re-running it will make new OpenAI API calls.
- `lr_scraper_estimator.ipynb` documents the original scraping, LR parsing, model-querying, and workbook-collation workflow. Network and API-writing steps are disabled by default with explicit `RUN_*` flags. Re-running model-query cells requires an OpenAI API key and may produce model-version or sampling differences.

### Workbooks

- `NNT_LRs_08-26-2025.xlsx` is the manually validated analysis workbook used by the manuscript analyses.
- `nnt_lrs_with_estimated.xlsx` contains model-generated LR columns and condition-sheet labels used by the analysis notebooks.

## Environment Setup

This project uses `uv` with a pinned lockfile.

```bash
pip install uv
uv sync
uv run jupyter lab
```

To run notebooks that query the OpenAI API, create a local `.env` file:

```ini
OPENAI_API_KEY=sk-...
```

Do not commit `.env`. The tracked `.env.example` file documents the required variable name only.

## Reproducing Analyses

The main manuscript and additional requested analyses notebooks can be executed without API calls because they use the included workbooks.

```bash
uv run jupyter nbconvert --to notebook --execute data_analysis.ipynb \
  --output tmp/data_analysis_executed.ipynb

uv run jupyter nbconvert --to notebook --execute additional_requested_analyses.ipynb \
  --output tmp/additional_requested_analyses_executed.ipynb
```

The threshold perturbation notebook calls the OpenAI API when executed. The estimator notebook keeps live scraping, model-querying, and workbook-conversion steps behind `RUN_SCRAPE`, `RUN_OPENAI_BATCH`, and `RUN_CONVERT_TO_MASTER` flags. Treat fresh API outputs as new runs rather than exact regeneration of the manuscript workbook.

Generated outputs are written to dated folders under `results/`. This folder is ignored by Git because it contains derived artifacts.

## License and Citation

License: MIT. See `LICENSE`.

Please cite the accompanying manuscript and this repository when using these materials.
